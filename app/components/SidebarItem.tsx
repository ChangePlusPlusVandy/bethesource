"use client";
import styled from "styled-components";

// TODO: Add props for SidebarItem
type SidebarItemProps = {
  propName: string; // replace string with actual prop type
};

// TODO: implement SidebarItem component (extend the Button component and edit styling)
export default function SidebarItem({}: SidebarItemProps) {
  return <StyledSidebarItem />;
}

const StyledSidebarItem = styled.div`
  /* TODO: Add styles for SidebarItem */
`;
